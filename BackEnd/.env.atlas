# Environment
NODE_ENV=production
PORT=8080

# Your allowed frontends (add your live domain when you have it)
CORS_ORIGIN=http://localhost:5173,https://your-domain.com

# Replace <user>, <pass>, and cluster name with your Atlas details,
# and choose your DB name ("4s" here).
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster-name>.mongodb.net/4s?retryWrites=true&w=majority

# Optional
APP_TZ=Europe/Zagreb
LOG_LEVEL=info
UPLOAD_MAX_MB=50

# (only if you wire these later)
# JWT_ACCESS_SECRET=super-strong-random-string
# JWT_REFRESH_SECRET=another-strong-random-string
# S3_ENABLED=true
# S3_BUCKET=your-prod-bucket
# S3_REGION=eu-central-1
# S3_ACCESS_KEY_ID=...
# S3_SECRET_ACCESS_KEY=...
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@your-domain.com
