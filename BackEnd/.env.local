# Environment
NODE_ENV=development
PORT=8080

# Your React dev URL (comma-separate more origins if needed)
CORS_ORIGIN=http://localhost:5173

# Local MongoDB connection (Docker or installed)
MONGO_URI=mongodb://127.0.0.1:27017/fortis_dev

# Optional but handy
APP_TZ=Europe/Zagreb
LOG_LEVEL=debug
UPLOAD_MAX_MB=50

# (only if you wire these later)
# JWT_ACCESS_SECRET=dev-access-secret
# JWT_REFRESH_SECRET=dev-refresh-secret
# S3_ENABLED=false
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=